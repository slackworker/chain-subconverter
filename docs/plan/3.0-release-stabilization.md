# 3.0 发布整理 Plan

目标：分支/标签、工作流、文档、发布回归与固定测试基线可维护。进度见 [../progress/STATUS.md](../progress/STATUS.md)。

## 1. 当前基线

- 默认入口 `/`（`default`）；`/ui/a|b|c` 为实验入口。
- 分支：`dev`（`dev-latest` 手动）、`beta`（`beta-latest`）、`main`（`latest`）。
- 固定 fixture（并列）：
  - [3pass-ss2022-test-subscription](../testing/3pass-ss2022-test-subscription.md)
  - [dual-landing-chain-port-forward](../testing/dual-landing-chain-port-forward.md)

## 2. 工作流

| ID | 目标 | 主要文件 |
|---|---|---|
| W0 | 分支与滚动标签 | `.github/workflows/*` |
| W1 | README / RELEASES / deploy | 仓库根与 `deploy/` |
| W2 | STATUS / plan / ROADMAP / runbook | `docs/` |
| W3 | 回归记录与 Issue 模板 | [release-runbook](../testing/release-runbook.md), `.github/ISSUE_TEMPLATE/` |
| W4 | 固定回归基线 | `internal/review/testdata/`, `docs/testing/` |

## 3. 状态

### W0 — 已完成

工作流与本地 `beta` 分支已就绪；`deploy/docker-compose.yml` 默认镜像已改为 `latest`。

### W1 — 基本完成

README / RELEASES / deploy 已与滚动标签对齐；随回归记录继续微调表述即可。

### W2 — 进行中

本轮已同步 beta-readiness、plan、SECURITY、local-dev-smoke 等。`docs/temp/` 本地讨论稿已清空（仅保留 [README](../temp/README.md)；阻断 E2E 原型缺口已写入 [test-system-review](../testing/test-system-review.md)）。

### W3 — 已完成

runbook 含回归模板；Issue 模板已补。**2026-05-23** 第三方设备回归已归档：内网 / 公网 / 双 Docker 三种形态均 **通过**（见 [third-party-deployments](../testing/third-party-deployments.md)）。

### W4 — 核心已完成

双落地 fixture 与长/短链 service/review 测试已落地。待办：阻断路径 E2E、更广 UI 矩阵（非 Beta 硬阻塞）。

## 4. 推荐顺序

1. ~~Beta 首版~~：`v3.0.0-beta.1` 已发布（2026-05-24，见 [RELEASES.md](../../RELEASES.md)）。
2. ~~**beta.2**~~：`v3.0.0-beta.2` 已发布（2026-05-25）；vps-01/02 实战回归已更新 digest（见 [third-party-deployments](../testing/third-party-deployments.md)）。
3. W4 剩余：E2E 阻断路径（可选进 CI）；`include-exclude-filter` 尚未纳入 `test:e2e:mock` blocking 列表。
4. Beta 线稳定后：将本 plan 剩余条目并入 [STATUS](../progress/STATUS.md) 并删除本文件。
5. ~~W2：清理 `docs/temp/`~~（2026-05-22 已完成）。
6. ~~W3：Beta 发布演练 + 回归记录归档~~（2026-05-23 已完成，见 [third-party-deployments](../testing/third-party-deployments.md)）。

## 5. 不在本轮

- 新大功能；强制版本号体系；spec 02–04 业务规则重写；`subconverter` digest 固定（回归注明 tag 即可）。
