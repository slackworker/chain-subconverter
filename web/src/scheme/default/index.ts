import type { UISchemeDefinition } from "../../lib/composition";

export const defaultUIScheme: UISchemeDefinition = {
	id: "default",
	label: "UI Default",
	description: "默认 UI（发布默认入口）。",
	interactionTier: "baseline",
	primaryBlockingFeedbackPlacement: "stage-local",
};
